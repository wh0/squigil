{
	"version": "1.1.0",
	"type": "module",
	"private": true,
	"dependencies": {
		"@fastly/js-compute": "^3.35.1"
	},
	"devDependencies": {
		"@fastly/cli": "^12.1.0"
	},
	"scripts": {
		"build": "js-compute-runtime --module-mode ./src/index.js ./bin/main.wasm",
		"start": "fastly compute serve",
		"deploy": "fastly compute publish"
	}
}
